
login to RENREN, go to album, scroll down to the end of album page
copy html (F12, document.body.innerHTML) into a local file, store these files into "source folder path"

tsc
node index.js "source folder path" "target folder path"
